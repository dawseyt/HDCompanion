<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="winMessageBox" Title="Information" Width="450" SizeToContent="Height" MinHeight="180" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="{Theme_Bg}" CornerRadius="8" BorderBrush="{Theme_BtnBorder}" BorderThickness="1" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Grid x:Name="TitleBar" Grid.Row="0" Margin="20,20,20,0" Background="Transparent" Cursor="Hand">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Path x:Name="pathIcon" Grid.Column="0" Width="20" Height="20" Stretch="Uniform" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblTitle" Grid.Column="1" Text="Information" FontSize="16" FontWeight="SemiBold" Foreground="{Theme_Fg}" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </Grid>
            
            <TextBox x:Name="txtMessageBody" Grid.Row="1" Margin="52,12,20,20" IsReadOnly="True" Background="Transparent" BorderThickness="0" TextWrapping="Wrap" Foreground="{Theme_SecFg}" FontSize="13" VerticalScrollBarVisibility="Auto" MaxHeight="300" HorizontalScrollBarVisibility="Disabled"/>
            
            <Border Grid.Row="2" Background="{Theme_BtnBg}" CornerRadius="0,0,8,8" Padding="16,12" BorderThickness="0,1,0,0" BorderBrush="{Theme_BtnBorder}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnYes" Content="Yes" Width="80" Height="28" Margin="0,0,8,0" Visibility="Collapsed" Background="{Theme_PrimaryBg}" Foreground="{Theme_PrimaryFg}" BorderThickness="0"/>
                    <Button x:Name="btnNo" Content="No" Width="80" Height="28" Margin="0,0,0,0" Visibility="Collapsed" Background="{Theme_Bg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                    <Button x:Name="btnOk" Content="OK" Width="80" Height="28" Margin="0,0,8,0" Visibility="Collapsed" Background="{Theme_PrimaryBg}" Foreground="{Theme_PrimaryFg}" BorderThickness="0"/>
                    <Button x:Name="btnCancel" Content="Cancel" Width="80" Height="28" Margin="0,0,0,0" Visibility="Collapsed" Background="{Theme_Bg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>