<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remote Printer Install" Width="420" SizeToContent="Height" WindowStartupLocation="CenterOwner" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.8"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{Theme_BtnBg}"/>
            <Setter Property="Foreground" Value="{Theme_Fg}"/>
            <Setter Property="BorderBrush" Value="{Theme_BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="{Theme_Bg}" CornerRadius="8" BorderBrush="{Theme_BtnBorder}" BorderThickness="1" Margin="15">
        <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Border x:Name="TitleBar" Grid.Row="0" Background="Transparent" Padding="16,16,16,8" Cursor="Hand">
                <TextBlock x:Name="lblHeaderTitle" Text="Install Printer" FontSize="16" FontWeight="SemiBold" Foreground="{Theme_Fg}"/>
            </Border>
            
            <StackPanel Grid.Row="1" Margin="16,8,16,16">
                <TextBlock Text="Printer Name (Friendly Name):" FontSize="11" Foreground="{Theme_SecFg}" Margin="0,0,0,4"/>
                <TextBox x:Name="txtName" Height="30" Margin="0,0,0,12"/>
                
                <TextBlock Text="Driver Name (Select or Type):" FontSize="11" Foreground="{Theme_SecFg}" Margin="0,0,0,4"/>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="32"/>
                        <ColumnDefinition Width="45"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="cbDriver" Grid.Column="0" Height="30" IsEditable="True" Margin="0,0,4,0"/>
                    <Button x:Name="btnUploadDriver" Grid.Column="1" Content="File" FontWeight="Bold" ToolTip="Upload and Stage Driver from local .INF file" Background="{Theme_BtnBg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1" Margin="0,0,2,0"/>
                    <Button x:Name="btnLocalDriver" Grid.Column="2" Content="Local" FontSize="10" FontWeight="Bold" ToolTip="Copy installed driver from this PC" Background="{Theme_BtnBg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                </Grid>
                
                <TextBlock Text="IP Address:" FontSize="11" Foreground="{Theme_SecFg}" Margin="0,0,0,4"/>
                <TextBox x:Name="txtIP" Height="30"/>
            </StackPanel>
            
            <Border Grid.Row="2" Background="{Theme_BtnBg}" CornerRadius="0,0,8,8" Padding="16,12" BorderThickness="0,1,0,0" BorderBrush="{Theme_BtnBorder}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnInstall" Content="Install" Width="80" Height="28" Margin="0,0,8,0" Background="{Theme_PrimaryBg}" Foreground="{Theme_PrimaryFg}" BorderThickness="0" IsDefault="True"/>
                    <Button x:Name="btnCancel" Content="Cancel" Width="80" Height="28" IsCancel="True" Background="{Theme_Bg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>